<div class="row">
  <% @ambassadors.each do |ambassador|%>
  <div class="col-md-12">
    <%= link_to image_tag(ambassador.picture.url, class: "img-fluid img-thumbnail", alt: ambassador.name), ambassador_path(ambassador) if ambassador.picture? %>
  <% end %>
</div>
