<%= simple_form_for(@evento) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :info %>
    <%= f.input :img %>
    <%= f.input :titulo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
