import { Controller } from "stimulus"

export default class extends Controller {
  static targets = [ "name", "output" ]


  initialize() {
  }
  //Send new info in the same hash format since the last moment

}
